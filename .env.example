# Host port published by the api service (container port is always 8000).
# Copy to .env (ignored by git) and adjust as needed.
API_PORT=8000

# SQLite database file for the quote module (optional; defaults to
# quotes.sqlite3 in the process working directory). Migrations run at
# application startup in the same process.
# QUOTE_DB_PATH=quotes.sqlite3
